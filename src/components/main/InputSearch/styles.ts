import styled from "styled-components";

export const InputSearch = styled.input`
  font-weight: 500;
  font-size: 0.9em;
  border: none;
  background: transparent;
  outline: none;
  margin-inline: auto;
  text-align: center;
`;
