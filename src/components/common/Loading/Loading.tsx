import React from "react";
import { LoadingBar, LoadingContainer } from "./styles";

const LoadingComponent = () => (
  <LoadingContainer>
    <LoadingBar />
  </LoadingContainer>
);

export default LoadingComponent;
